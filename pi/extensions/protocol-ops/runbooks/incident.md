Start with the smallest useful observation set, then branch from evidence.

1. Confirm the exact target, symptom, monitoring timestamp, and blast radius.
2. Collect baseline health, failed units, and relevant process state. Request `network` or a sensitive log profile only when the earlier evidence makes it discriminating.
3. Separate confirmed facts from hypotheses. Correlate timestamps before assigning causality.
4. If a required read is not in the audited catalog, say which named check is missing. Do not smuggle arbitrary shell through another tool.
5. Checkpoint after discovery with facts, uncertainty, and the next discriminating observation.
